services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: gdgc
      POSTGRES_PASSWORD: dev_password
      POSTGRES_DB: gdgc_recruitment
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
